Java:
------------------------------------------------

Stack.peek() will give exception if the stack is empty

LinkedList has all the similar fuctions as stack but it doesn't implement stack interface so 
Stack<Integer> s = new LinkedList(); will result in exception...
